version: '3.8'
services:
  nginx:
    image: nginx:alpine
    container_name: erp_nginx
    ports:
      - "8085:8085"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
